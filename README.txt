To run this app you need to:

node index.js <product name> <product quantity>

The product names must match the names we have in the products.js file and the quantity must be a valid number.

Also the order is important, it will not work if you enter quantity followed by product name